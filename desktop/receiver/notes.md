If installation fails on Windows:
- Upgrade pip: `python -m pip install --upgrade pip`
- Let pip resolve compatible av version (aiortc 1.9 requires av<13): `python -m pip install -r requirements.txt`
- Ensure OBS Virtual Camera is installed and registered for pyvirtualcam.
