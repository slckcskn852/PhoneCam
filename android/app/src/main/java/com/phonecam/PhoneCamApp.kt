package com.phonecam

import android.app.Application

class PhoneCamApp : Application() {
    override fun onCreate() {
        super.onCreate()
        // Minimal application initialization kept for manifest reference.
    }
}
